langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_featured
    - node.type.project
id: node.project.field_featured
field_name: field_featured
entity_type: node
bundle: project
label: Featured
description: 'Select this to display this project as the featured item on project pages. If multiple items are selected, the most recently created item will appear.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
