# Acquia Trials Countdown
Provides a banner that counts down to the end of a trial period for Acquia Cloud. The banner is displayed on all pages of the site.

The trial expiry is hardcoded in acquia_trials_countdown_fetch_trial_end(). That should be updated t fetch from an API once the spec is defined.
