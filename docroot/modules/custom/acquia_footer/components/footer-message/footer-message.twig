<section{{ attributes }}>
  {% if title %}
    <h2 class="footer-message__title">{{ title }}</h2>
  {% endif %}
  {% if body %}
    <div class="footer-message__body">{{ body }}</div>
  {% endif %}
</section>
