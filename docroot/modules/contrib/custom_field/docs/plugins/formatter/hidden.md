# Hidden formatter
The **Hidden** (`hidden`) formatter hides the field value from field display. There are no
additional settings and this formatter is available for all field types.

> [!tip]
> This formatter may be useful for hiding sensitive information, fields that
> are not intended to be displayed, or fields that are part of a custom field
> reused across multiple entity type bundles with different display intentions.
