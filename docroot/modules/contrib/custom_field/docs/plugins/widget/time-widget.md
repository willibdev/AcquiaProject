# Time widget
The **Time** (`time_widget`) widget allows the user to select a time with the 
`<input type="time">` form element. Depending on browser support, a time picker
can show up in the input field.

## Field types
- [time](../type/time.md)
