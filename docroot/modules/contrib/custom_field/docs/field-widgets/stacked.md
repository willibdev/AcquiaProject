# Stacked widget
The **Stacked** widget (`custom_stacked`) is the default widget. It displays
each subfield in a vertical stack, exactly like standard Drupal fields. This
layout is ideal when you want full-width editing controls and maximum clarity
for complex or nested data.

--8<-- "field_widget.md:settings"