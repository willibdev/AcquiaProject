.canvas-ui: &canvas-ui
  - npm ci
  # The UI's build script runs through Turborepo, which builds its workspace
  # dependencies (headless, astro-hydration, and their dependencies) first.
  - npm run build -w ui
