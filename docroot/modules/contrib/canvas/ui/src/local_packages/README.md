This provides the functionality that makes it possible for the component instance and
content entity forms to render React components based on markup in Twig.

Maybe move this eventually to somewhere else. Perhaps in an npm/jsr registry if
there's a benefit to having this available in different projects.

