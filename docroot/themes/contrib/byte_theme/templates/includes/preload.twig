{#
/**
 * @file
 * Preload the commonly used fonts for the headings and the body copy.
 *
 * Available variables:
 * - byte_theme_path: Returns the path to the Byte Theme.
 */
#}

<link rel="preload" href="{{ byte_theme_path }}/fonts/Roboto/roboto-var-latin.woff2" as="font" type="font/woff2" crossorigin />
