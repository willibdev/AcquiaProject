$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: Partner Card
group: Molecules
status: stable
description: An individual logo component with a grey card background for partners.
props:
  type: object
  properties:
    admin_theme:
      title: Admin Theme
      type: boolean
      default: false
      description: Check to enable Admin Theme styles (applies CSS only on frontend to preserve Canvas builder look).
slots:
  media:
    title: Media Slot
    description: Slot for the logo image.
library:
  css:
    component:
      partner-card.css: {}
