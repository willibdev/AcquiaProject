{% set classes = [
  'location-split',
  admin_theme ? 'location-split--admin-theme' : 'location-split--themed',
] %}

<section{{attributes.addClass(classes)}}>
  <div class="location-split__container">
    
    <div class="location-split__left">
      {% if eyebrow %}
        <div class="location-split__eyebrow">
          {{ eyebrow }}
        </div>
      {% endif %}
      
      {% if title %}
        <h2 class="location-split__title">
          {{ title }}
        </h2>
      {% endif %}
    </div>

    <div class="location-split__right">
      {% if description %}
        <div class="location-split__description">
          {{ description }}
        </div>
      {% endif %}
      
      <div class="location-split__details">
        {% if address %}
          <div class="location-split__detail-item location-split__detail-item--address">
            <span class="location-split__icon">
              {# Map Pin SVG #}
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
                <circle cx="12" cy="10" r="3"></circle>
              </svg>
            </span>
            <span class="location-split__text">{{ address }}</span>
          </div>
        {% endif %}

        {% if phone %}
          <div class="location-split__detail-item location-split__detail-item--phone">
            <span class="location-split__icon">
              {# Phone SVG #}
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
              </svg>
            </span>
            <span class="location-split__text">{{ phone }}</span>
          </div>
        {% endif %}
      </div>

      <div class="location-split__action">
        {{ action }}
      </div>
    </div>

  </div>
</section>
