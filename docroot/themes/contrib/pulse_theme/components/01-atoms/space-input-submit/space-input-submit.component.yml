$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json

name: Space Form - Submit

description: A generic submit component for various HTML input types (submit).

status: obsolete

group: Atoms

props:

  type: object

  properties:
    attributes:
      type: Drupal\Core\Template\Attribute
      title: Attributes
      description: HTML attributes for the input element.

    type:
      type: string
      title: Type
      description: The HTML input type (e.g., submit).
